version: 1
auto-generated: true
indices:
  default:
    include:
      - /**
    target: /query-index.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
  events:
    include:
      - /events/**
    target: /events/query-index-events.json
    properties:
      lastModified:
        select: none
        value: parseTimestamp(headers["last-modified"], "ddd, DD MMM YYYY hh:mm:ss GMT")
      title:
        select: head > meta[property="og:title"]
        value: attribute(el, "content")
      description:
        select: main > div > div > p:nth-of-type(4)
        value: |
          attribute(el "content")
      test:
        select: head > meta[name="test"]
        value: attribute(el, "content")